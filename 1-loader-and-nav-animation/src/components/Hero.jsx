// import { useRef } from "react";
// import gsap from "gsap";
// import { useGSAP } from "@gsap/react";

const Hero = () => {
  // const heroRef = useRef(null);

  // useGSAP(() => {
  //   gsap.fromTo(
  //     heroRef.current,
  //     { opacity: 0, y: 50 },
  //     { opacity: 1, y: 0, duration: 1, ease: "power2.out", delay: 0.2 }
  //   );
  // }, []);

  return (
    // <div
    //   ref={heroRef}
    //   className="w-full h-[80vh] flex flex-col justify-center items-center bg-gradient-to-br from-gray-100 to-gray-300"
    // >
    //   <h1 className="text-5xl font-bold mb-4">.</h1>
    // </div>

    <div className="w-full h-90vh flex bg-gray-400">
      
    </div>
  );
};

export default Hero;
